{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd my-nestjs-backend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd my-nestjs-backend && node dist/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}